{
  "name": "cricetus",
  "version": "1.0.0",
  "description": "electron app",
  "main": "./src-tsc/main.js",
  "scripts": {
    "postinstall": "cd web && npm install",
    "start": "npm run start:react",
    "pack": "npm run pack:react",
    "dist": "npm run dist:react",
    "build:react": "cd web && npm run build",
    "start:react": "tsc && concurrently \"cross-env STAGE=development FRAME=react electron .\" \"cd web && npm run dev\"",
    "pack:react": "tsc && cd web && npm run build && cd .. && cross-env FRAME=react electron-builder --dir",
    "dist:react": "tsc && cd web && npm run build && cd .. && electron-builder"
  },
  "author": "cricetus",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "cross-env": "^7.0.3",
    "electron": "^16.0.1",
    "electron-builder": "^22.14.5",
    "typescript": "^4.5.5"
  },
  "build": {
    "appId": "com.cricetus.app",
    "productName": "仓鼠小应用",
    "icon": "./public/favicon.ico"
  }
}
